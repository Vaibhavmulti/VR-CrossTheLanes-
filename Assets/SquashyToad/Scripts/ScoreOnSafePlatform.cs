using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreOnSafePlatform : MonoBehaviour {
    /* 
    Just a Script for reference for scoring.
    Once we land on the ground we destroy this component so that we can only score once!!
    */
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
